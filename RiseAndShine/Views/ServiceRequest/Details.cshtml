@model RiseAndShine.Models.ViewModels.ServiceRequestUserProfileViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>
<hr />
<div>
    <h4>Service Requests</h4>
    <div class="container-fluid">
        <dd>
            <dl class="row">
                @foreach (ServiceRequest sr in Model.ServiceRequests)
                {
                    <div class="card m-6" style="width: 30rem height:20rem ;">
                        <div class="container">
                            <div class="row">
                                <div class="col-xs-12">
                                    <dd>
                                        <div>
                                            <label class="font-weight-bold">Service Provider:</label>
                                            <span>@sr.UserProfile.Name</span>
                                        </div>
                                        <div>
                                            <label class="font-weight-bold">Service Date:</label>
                                            <span>@sr.ServiceDate</span>
                                        </div>

                                        <div>
                                            <label class="font-weight-bold">Package Type:</label>
                                            @*@foreach (PackageType pt in Model.PackageTypes)
                                        {*@
                                            <select asp-for="@Model.PackageType.Id" style="color:dodgerblue" autofocus>
                                                <label class="font-weight-bold">price:</label>
                                                <option>Select A Package...</option>
                                            </select>
                                            @*}*@
                                        </div>

                                        <form method="post" asp-for="CardId" asp-route-Id="CarId" asp-controller="ServiceRequest" asp-action="Edit">
                                            <div>
                                                <input value="SELECT SERVICE REQUEST" type="submit" text-center class="btn btn-outline-dark" />
                                            </div>
                                        </form>

                                        <div>
                                        </div>

                                    </dd>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </dl>
        </dd>
        <div>
        </div>
        <div>

            <a asp-action="Details">Back to List</a>
        </div>
